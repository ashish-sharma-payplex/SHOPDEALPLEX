import { styled } from "@mui/material";
import { Box } from "@mui/system";

const HomeComponentsWrapper = styled(Box)(({ theme }) => ({
	width: "100%",
	gap: "8px",
}));

export default HomeComponentsWrapper;
